import React from 'react'
import styles from '@/styles/components/NoJobs.module.scss'

export default function NoJobs() {
  return (
    <div className={styles.noJobs}>No jobs found, <br /> press the + Add Job button ➚</div>
  )
}
